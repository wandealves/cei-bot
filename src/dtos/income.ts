export default interface Income {
  active: string;
  code: string;
  eventType: string;
  grossValue: number;
  netValue: number;
  paymentDate: string;
  quantity: number;
  quotationFactor: number;
  specification: string;
}
